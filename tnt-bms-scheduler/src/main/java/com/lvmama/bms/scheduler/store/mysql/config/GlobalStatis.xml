<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lvmama.bms.scheduler.store.mysql.mapper.GlobalStatisMapper">

	<insert id="save">
		  INSERT INTO tnt_bms_global_statistics
			(reach_count, receive_count, fail_count, send_count, `timestamp`)
		  VALUES (#{reachCount}, #{receiveCount}, #{failCount}, #{sendCount}, #{timestamp})
	</insert>


</mapper>